# PG-DIVERGENCE: in an aggregate with DISTINCT, ORDER BY expressions must appear in argument list
# PG-DIVERGENCE: in an aggregate with DISTINCT, ORDER BY expressions must appear in argument list
# PG-DIVERGENCE: in an aggregate with DISTINCT, ORDER BY expressions must appear in argument list
missing_col
