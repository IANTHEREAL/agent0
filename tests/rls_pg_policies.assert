# pg_policies view — verify human-readable output and correct types

# Query 3: all policies with correct columns, ordered by name
del_restrict|DELETE|RESTRICTIVE|(data IS NOT NULL)|NULL
ins_own|INSERT|PERMISSIVE|NULL|(user_id = CURRENT_USER)
sel_all|SELECT|PERMISSIVE|true|NULL
upd_own|UPDATE|PERMISSIVE|(user_id = CURRENT_USER)|(user_id = CURRENT_USER)

# Query 4: roles is array, cast to text shows {public}
sel_all|{public}

# Query 5: roles column type is name[]
name[]

# Query 6: schema name is public
public|pol_test

# Query 7: pg_policy polroles cast works
sel_all|{0}
